spring:
  datasource:
    url: jdbc:h2:mem:alertastest;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: clave-de-test-de-al-menos-32-caracteres-ok-000

catalogo:
  url: http://localhost:8081
  timeout-ms: 3000

logging:
  level:
    com.terrahome.msalertas: DEBUG
